import pandas - For handling CSVs, DataFrames, filtering, and displaying tables
import streamlit - Main framework to build the web dashboard and interactive forms
import pymysql - For connecting to MySQL / MariaDB databases and executing SQL queries
